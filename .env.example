# Google Drive backup (optional)
#
# Create an OAuth 2.0 Web Client ID in the Google Cloud Console, enable the
# Drive API, and add http://localhost:5173 (and your production origin) to
# the "Authorized JavaScript origins" list. Then paste the client id here:
#
# VITE_GOOGLE_CLIENT_ID=
